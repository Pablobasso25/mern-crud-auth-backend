export default {
  plugins: {
    "@tailwindcss/postcss": {}, // Este es el nombre del plugin para la versión 4
    autoprefixer: {}, // Añade prefijos para que el CSS funcione en todos los navegadores
  },
};
